import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.mpasssofia',
  appName: 'mpass',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
